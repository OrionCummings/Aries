def enum States {
    STATE0,
    STATE1,
    STATE2,
    STATE3,
    STATE4,
    STATE5,
    STATE6,
    STATE7,
    STATE8,
    STATE9,
};

// This is the main function! Maybe this will work?
def main(void) -> int {

    opt<i32> a = none;
    opt<i32> b = 27;

    i32? c = None;
    i32? d = -12789;

    i32 x = ?b + ?d;

    return 0;
}